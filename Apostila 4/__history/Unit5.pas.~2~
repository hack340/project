unit Unit5;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls;

type
  TForm5 = class(TForm)
    Label1: TLabel;
    Edit1: TEdit;
    Edit2: TEdit;
    Label2: TLabel;
    Label3: TLabel;
    Button1: TButton;
    Button2: TButton;
    procedure Button1Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form5: TForm5;

implementation

{$R *.dfm}

procedure TForm5.Button1Click(Sender: TObject);
var
  pessoas: real;
  peso: real;
begin
  pessoas:= strtofloat(edit1.text);
  peso:= strtofloat(edit2.text);

  if pessoas > 6 then
  begin
    ShowMessage('Capacidade Excedida! Máximo permitido: 6 Pessoas.')
  end
  else
  begin


end;

end.
